# Gerenciador de Produtos em C

Projeto simples em C que demonstra: structs, ponteiros, leitura/gravação em arquivo binário, modularização e uso de Makefile.

## Como compilar

```bash
make
Como executar
./gerenciador
Teste simples
gcc -g tests/test_produto.c src/produto.c -o test_produto
./test_produto
